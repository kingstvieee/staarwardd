{
  "name": "staarwardd-prototype",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "StaarWardd — a human-centred life orchestration prototype",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "amazon:alexa": "node amazon/alexa-mcp-server.mjs",
    "test": "node --test test/server.test.mjs",
    "check": "node --check server.mjs && node --check public/app.js && node --check amazon/alexa-mcp-server.mjs"
  },
  "engines": { "node": ">=20" }
}
